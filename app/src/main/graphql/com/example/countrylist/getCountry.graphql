query GetCountry($code: ID!){
    country(code: $code) {
        name
        capital
        continent {
            name
        }
        currency
        languages {
            name
        }
    }
}